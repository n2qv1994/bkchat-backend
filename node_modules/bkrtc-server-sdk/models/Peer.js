function Peer(peer_name, peer_id, connection_id, status) {
  var self = this;
  self.peer_id = peer_id;
  self.connection_id = connection_id;
  self.peer_name = peer_name;
  self.status = status; // status: {0: 'offline', 1:'online', 2:'busy'}
}

module.exports = Peer;
